query GetChecklists {
  checklists(orderBy: createdAt_DESC) {
    company
    duration
    id
    name
    createdAt
    result
    slug
    logo {
      url
    }
    imagePost {
      url
    }
  }
}
